import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['i.postimg.cc', 'media.dodostatic.net'], // Добавьте этот хост
  },
};

export default nextConfig;
